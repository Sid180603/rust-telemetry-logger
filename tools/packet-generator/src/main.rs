//! `packet-generator` — test frame emitter.
//!
//! Generates valid, corrupt, truncated, and bursty COBS-framed telemetry
//! frames and streams them to a file, TCP socket, UDP socket, or stdout.
//! Filled in during Phase 2.

fn main() {
    println!("packet-generator stub — Phase 2 will implement this.");
}
