//! `telemetry-linux` — embedded Linux systemd daemon.
//!
//! serialport source → [`Pipeline`] → [`FileStorage`] + sd-notify readiness.
//! Filled in during Phase 3.

fn main() {
    println!("telemetry-linux stub — Phase 3 will implement this.");
}
