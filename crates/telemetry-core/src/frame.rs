//! `Frame` and `Header` data types produced by the framer.
//!
//! These are *data* types only — no parsing logic lives here.
//! Parsing lives in [`crate::framer`]; encoding lives in `frame::encode`.
