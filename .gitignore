/target
**/*.rs.bk
.DS_Store
Thumbs.db

# cargo-fuzz artifacts
fuzz/target

# IDE
.vscode/
.idea/
*.iml
*.swp
*.swo

# Merge / backup leftovers
*.orig

# Code coverage & profiling artifacts (cargo-llvm-cov, tarpaulin, perf)
*.profraw
*.profdata
/coverage/
tarpaulin-report.*
cobertura.xml
lcov.info

# Yocto build artifacts (these live in yocto/ but can be large)
yocto/build/
yocto/downloads/
yocto/sstate-cache/

# Local env / secrets
.env

# Claude Code shell snapshots (created by Claude Code CLI, not part of project)
*shell-snapshots*
snapshot-bash-*.sh

# insta snapshot review files (pending diffs — not committed, run `cargo insta review`)
**/*.snap.new

# Runtime log output from telemetry-host / telemetry-linux
logs/
*.bin.log
